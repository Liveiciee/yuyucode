import { describe, it, expect } from 'vitest';

describe.skip('injectVisionImage', () => {
  it('does nothing when imageBase64 is null/undefined', () => {
    // Skip until injectVisionImage is properly exported
    expect(true).toBe(true);
  });
});
